const screenWidth = window.innerWidth;
const screenHeitgh = window.innerHeight;
const canvas = document.querySelector(".canvas") as HTMLCanvasElement;

const score = document.querySelector(".score") as HTMLCanvasElement;

score.innerText = "Score muda aqui";
